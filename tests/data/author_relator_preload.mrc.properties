# Turn on change tracking for this record set so we can use it for testing change tracking.
# This file contains the same records as author_relators.mrc, but with some modified to have
# earlier 005 dates. This file will be indexed first based on its alphabetical filename, and
# then all records will be overwritten by the versions in author_relators.mrc. This allows us
# to test change tracking behavior.
first_indexed = custom, getFirstIndexed(001)
last_indexed = custom, getLastIndexed(001)
