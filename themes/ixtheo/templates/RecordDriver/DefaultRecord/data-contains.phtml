<?php foreach ($data as $containedTitle): ?>
  <?php if (!empty($containedTitle['id'])): ?>
    <a href="<?=$this->recordLinker()->getUrl($containedTitle['id'])?>"><?=$containedTitle['description']?></a>
  <?php else: ?>
    <?=$containedTitle['description']?>
  <?php endif; ?>
  <br>
<?php endforeach; ?>