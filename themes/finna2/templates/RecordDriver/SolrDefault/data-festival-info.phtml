<? //Don't add START and END comments ?>
<? foreach($data as $fields => $keys): ?>
  <? if (!empty($keys['name'])): ?>
    <?=$this->escapeHtml($keys['name'])?>
      <? if (!empty($keys['region'])): ?>
        <?=$this->escapeHtml($keys['region'])?>
      <? endif; ?>
    <? if (!empty($keys['date'])): ?>
       <?=$this->escapeHtml($keys['date'])?>
    <? endif; ?><br/>
  <? endif; ?>
<? endforeach; ?>
