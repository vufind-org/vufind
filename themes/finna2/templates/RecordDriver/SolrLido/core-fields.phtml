<!-- START of: finna - RecordDriver/SolrLido/core-fields.phtml -->
<?php
  $formatter = $this->recordDataFormatter($this->driver);
  $coreFields = $formatter->getDefaults();

  // Note: groups contain also fields that come before the Context group:
  $coreFieldGroups = $formatter->getGroupedData(
      $this->driver,
      $formatter->getGroupedFields(
          [
              ['lines' => ['Published in']],
              ['lines' => ['Format']],
              ['lines' => ['Format and Labels']],
              ['lines' => ['Organisation']],
              ['lines' => ['Collection']],
              [
                'label' => 'Context',
                'lines' => ['Parent Archive', 'Parent Collection', 'Parent Series', 'Parent Work'],
              ],
              ['lines' => ['Identifiers']],
              ['lines' => ['Inventory ID']],
              ['lines' => ['Language']],
              ['lines' => ['Extent']],
              ['lines' => ['original_work_language']],
              ['lines' => ['Other ID']],
              ['lines' => ['Location LIDO']],
              ['lines' => ['Measurements']],
              ['lines' => ['Inscriptions']],
              ['lines' => ['Events']],
              ['lines' => ['Edition']],
              ['lines' => ['lido_editions']],
              ['lines' => ['Subject Detail']],
              ['lines' => ['Subject Place']],
              ['lines' => ['Subject Date']],
              ['lines' => ['Subject Actor']],
              ['lines' => ['SubjectsWithoutPlaces']],
              ['lines' => ['Other Classifications']],
              ['lines' => ['Other Classification']],
              ['lines' => ['Publications']],
              ['lines' => ['Introduction']],
              ['lines' => ['child_records']],
              ['lines' => ['ISBN']],
              ['lines' => ['ISSN']],
              ['lines' => ['DOI']],
              ['lines' => ['Author Notes']],
              ['lines' => ['Available Online']],
              ['lines' => ['Parent Unclassified Entity']],
          ],
          $coreFields
      )
  );
?>
<?php if (!empty($coreFieldGroups)): ?>
  <table class="table table-finna-record record-details record-field-groups" role="presentation" tabindex="-1">
    <?=$this->record($this->driver)->renderTemplate('core-field-groups.phtml', ['data' => $coreFieldGroups]);?>
  </table>
<?php endif; ?>
<!-- END of: finna - RecordDriver/SolrLido/core-fields.phtml -->
