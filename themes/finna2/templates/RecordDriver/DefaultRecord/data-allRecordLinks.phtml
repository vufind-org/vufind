<?php //Don't add START and END comments ?>
<?php if (!empty($data)): ?>
  <?php $prevRecordLinkTitle = null; ?>
  <?php foreach ($data as $recordLink): ?>
    <?php if ($recordLink['title'] !== $prevRecordLinkTitle): ?>
      <?php
        if ($prevRecordLinkTitle) {
          echo '</td></tr>';
        }
        echo '<tr class="recordLinks"><th>' . $this->transEsc($recordLink['title']) . ':</th><td>';
        $prevRecordLinkTitle = $recordLink['title'];
      ?>
    <?php endif; ?>
    <?='<a href="' . $this->recordLink()->related($recordLink['link']) . '">' . $this->escapeHtml($recordLink['value']) . '</a><br />'; ?>
  <?php endforeach; ?>
  <?php echo '</td>'; ?>
<?='</tr>' ?>
<?php endif; ?>
