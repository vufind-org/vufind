<?php //Don't add START and END comments ?>
<?php if (!empty($data)): ?>
  <div class="truncate-field" data-rows="5"> 
    <?php foreach ($data as $field): ?>
      <div class="child-collection contained-in">
        <?php if (!empty($field['id'])): ?>
          <a class="linked-record" href="<?=$this->escapeHtmlAttr($this->recordLinker()->getUrl($field['id']));?>"><?=$this->escapeHtml($field['title']);?></a>
        <?php else: ?>
          <?=$this->escapeHtml($field['title']) ?>
        <?php endif; ?>
      </div>
    <?php endforeach; ?>
  </div>
<?php endif; ?>
