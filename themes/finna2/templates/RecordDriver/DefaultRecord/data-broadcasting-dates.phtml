<?php //Don't add START and END comments ?>
<div class="truncate-field">
  <?php foreach ($data as $fields => $keys): ?>
    <?php if (!empty($keys['time'])): ?>
       <?=$this->escapeHtml($keys['place'])?>, <?=$this->escapeHtml($keys['time'])?><?php if (!empty($keys['viewers'])): ?>, <?=$this->transEsc('Amount of Viewers') ?>: <?=$this->escapeHtml($keys['viewers']);?> <?php endif;?><br/>
    <?php endif; ?>
  <?php endforeach; ?>
</div>
