<!-- START of: finna - externalauth/ezproxylogin.phtml -->
<?
    // Set page title
    $this->headTitle($this->translate('external_auth_heading'));
    $this->layout()->finnaMainHeader = '<div><h1 class="content-header">' . $this->translate('external_auth_heading') . '</h1></div>';
?>

<div class="external-content-access">
  <div>
    <?=$this->flashmessages()?>
    <? if ($this->unauthorized): ?>
      <div class="unauthorized-description">
        <?=$this->transEsc('external_auth_unauthorized_desc'); ?>
      </div>
      <div>
        <a href="<?=$this->url('myresearch-logout')?>" class="logout btn btn-primary" title="<?=$this->transEsc("Log Out")?>"><strong><i class="fa fa-sign-out"></i> <?=$this->transEsc("Log Out")?></strong></a>
      </div>
    <? endif; ?>
  </div>
</div>

<!-- END of: finna - externalauth/ezproxylogin.phtml -->
