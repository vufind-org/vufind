<?php
  $pageTitle = $this->translate('ReservationList::Add To Reservation List');
  // Set up page title:
  $this->headTitle($this->translate($pageTitle));
  $user = $this->auth()->getUserObject();

  // Set up breadcrumbs:
  $this->breadcrumbs()->set(...$this->recordLinker()->getBreadcrumbParams($this->driver))
    ->add($pageTitle, active: true);

?>
<h2><?= $this->escapeHtml($pageTitle); ?></h2>
<div>
  <div>
    <label for="resource_name"><?=$this->transEsc('ReservationList::Resource')?>:</label>
    <p id="resource_name"><?=$this->transEsc($this->params['recordId'])?></p>
  </div>
  <div>
    <label for="resource_institution"><?=$this->transEsc('ReservationList::Resource Institution')?>:</label>
    <p id="resource_institution"><?=$this->transEscWithPrefix('source_', $this->params['institution']);?></p>
  </div>
</div>
<?php if ($this->reservationList()->singleOrderEnabled()): ?>
  <div class="btn-group">
    <a class="btn btn-primary" href="<?=$this->url('reservationlist-placesingleorder', ['id' => \Finna\ReservationList\Form\Form::RESERVATION_LIST_REQUEST], ['query' => $this->params])?>"><?=$this->transEsc('ReservationList::Reserve only this item')?></a>
  </div>
<?php endif; ?>
<div class="btn-group">
  <a class="btn btn-primary" href="<?=$this->url('reservationlist-additemtolist', [], ['query' => $this->params])?>"><?=$this->transEsc('ReservationList::Add To Reservation List')?></a>
</div>
