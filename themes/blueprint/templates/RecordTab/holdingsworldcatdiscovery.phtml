<? $holdings = $this->driver->getOtherLibraryOffers(); 
if ($holdings && count($holdings) > 0): ?>
<h3><?=$this->transEsc('Holdings at Other Libraries')?></h3>
<ul>
<? foreach ($holdings as $holding): ?>
<li><?= $holding->getSeller()->getName()?></li>
<? endforeach; ?>
</ul>
<? endif; ?>