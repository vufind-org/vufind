<div>
  <? if (!empty($this->electronic)): ?>
    <div class="openurls">
      <strong><?=$this->transEsc('Electronic')?></strong>
      <ul>
        <? foreach ($this->electronic as $link): ?>
          <li>
            <? if (isset($link['href']) && !empty($link['href'])): ?>
              <a href="<?=$this->escape($link['href'])?>" title="<?=isset($link['service_type'])?$this->escape($link['service_type']):''?>"><?=isset($link['title'])?$this->escape($link['title']):''?></a> <?=isset($link['coverage'])?$this->escape($link['coverage']):''?>
            <? else: ?>
              <?=isset($link['title'])?$this->escape($link['title']):''?> <?=isset($link['coverage'])?$this->escape($link['coverage']):''?>
            <? endif; ?>
          </li>
        <? endforeach; ?>
      </ul>
    </div>
  <? endif; ?>
  <? if (!empty($this->print)): ?>
    <div class="openurls">
      <strong><?=$this->transEsc('Holdings')?></strong>
      <ul>
        <? foreach ($this->print as $link): ?>
          <li>
            <? if (isset($link['href']) && !empty($link['href'])): ?>
              <a href="<?=$this->escape($link['href'])?>" title="<?=isset($link['service_type'])?$this->escape($link['service_type']):''?>"><?=isset($link['title'])?$this->escape($link['title']):''?></a> <?=isset($link['coverage'])?$this->escape($link['coverage']):''?>
            <? else: ?>
              <?=isset($link['title'])?$this->escape($link['title']):''?> <?=isset($link['coverage'])?$this->escape($link['coverage']):''?>
            <? endif; ?>
          </li>
        <? endforeach; ?>
      </ul>
    </div>
  <? endif; ?>
  <div class="openurls">
    <strong><a href="<?=$this->escape($this->openUrlBase)?>?<?=$this->escape($this->openUrl)?>"><?=$this->transEsc('More options')?></a></strong>
    <? if (!empty($this->services)): ?>
      <ul>
        <? foreach ($this->services as $link): ?>
          <? if (isset($link['href']) && !empty($link['href'])): ?>
            <li>
              <a href="<?=$this->escape($link['href'])?>" title="<?=isset($link['service_type'])?$this->escape($link['service_type']):''?>"><?=isset($link['title'])?$this->escape($link['title']):''?></a>
            </li>
          <? endif; ?>
        <? endforeach; ?>
      </ul>
    <? endif; ?>
  </div>
</div>
