<?php if (!empty($data)): ?>
  <?php list($ppn, $preceding_title) = explode(':', $data, 2); ?>
  <a href="<?=$this->recordLink()->getUrl($ppn)?>"><?=htmlspecialchars($preceding_title)?></a>
<?php endif; ?>
