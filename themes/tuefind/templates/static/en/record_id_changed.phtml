<h1>URL (ID) has changed</h1>
<p>The ID you are looking for (<?=$this->originalId?>) is no longer valid, please use the following ID instead: <a href="<?=$this->recordLinker()->getUrl($this->driver->getUniqueID())?>"><?=$this->driver->getUniqueID()?></a></p>
<p>This notification will be disabled on January 1st, 2020. Please update your saved old id references (e.g. bookmarks in your browser) as soon as possible.</p>
<p>Ideally you shouldn't save items by bookmarking their URL's. Instead, you should login and add them to your favourites list. This will also guarantee that any future changes will not affect you.</p>
