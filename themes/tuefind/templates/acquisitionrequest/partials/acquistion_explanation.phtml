<? $translated_description = $this->transEsc('acquisition_request_description', ['%%FIDType%%' => $this->FIDType,
                                             '%%TueFindType%%' => $this->TueFindType ]);?>
<? echo $translated_description;?>
<? // Email explanation only for krimdok since mandatory otherwise ?>
<? if ($this->TueFindType == 'Krimdok')
       echo " " . $this->transEsc('acquisition_email_explanation_optional');
   else
       echo " " . $this->transEsc('acquisition_email_explanation');
?>

