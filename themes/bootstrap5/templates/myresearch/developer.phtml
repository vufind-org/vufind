<?php
    $apiKey = $this->apiKey ?? [];
    $revoked = $apiKey['revoked'] ?? false;
?>
<h2><?=$this->transEsc('Developer::settings')?></h2>

<table class="table table-striped">
    <tr>
        <th class="vertical-center">
            <?=$this->transEsc('Developer::api_key')?>
        </th>
        <td class="flex align-center justify-end">
            <div>
                <?php if ($revoked): ?>
                    <span><?=$this->transEsc('Developer::api_key_locked')?></span>
                <?php else: ?>
                    <a class="btn btn-primary" href="<?=$this->url('myresearch-generateapikey')?>"><?=$this->transEsc('Developer::api_key_generate')?></a>
                    <?php if ($apiKey): ?>
                        <a class="btn btn-secondary" href="<?=$this->url('myresearch-deleteapikey')?>"><?=$this->transEsc('Developer::api_key_delete')?></a>
                    <?php endif; ?>
                <?php endif; ?>
            </div>
        </td>
    </tr>
</table>
