<?php
$link = $getThisParams['open_hours'] ?? '';
if (empty($link)) {
    $linkStart = '';
    $linkEnd = '';
} else {
    $linkStart = '<a href="' . $this->escapeHtml($link) . '">';
    $linkEnd = '</a>';
}
?>
<div>
  <p>
    <strong><?= $this->transEsc('viewing_request') ?>:</strong>
    <?= $this->transEsc('microform_viewing_request') ?>
  </p>
</div>

