<?php
  $this->breadcrumbs()->disable();
  $title = $this->translate('checkout_item')
    . ': <span class="title-in-heading">' . $this->escapeHtml($this->driver->getBreadcrumb()) . '</span>';
?>
<?=
  // Set page title and heading.
  $this->component('page-title', [
    'title' => $title,
    'level' => 1,
    'headTitle' => $this->translate('checkout_item'),
    'escape' => false,
  ])
?>
<?php if (!$this->translationEmpty('checkout_confirm_html')): ?>
  <p>
    <?=$this->translate('checkout_confirm_html')?>
  </p>
<?php endif; ?>
<form class="form-checkout-item" method="post">
  <?=$this->flashmessages()?>
  <input type="hidden" name="id" value="<?=$this->escapeHtmlAttr($this->driver->getUniqueId())?>">
  <input type="hidden" name="source" value="<?=$this->escapeHtmlAttr($this->driver->getSourceIdentifier())?>">
  <div class="form-group">
    <input type="submit" class="btn btn-primary" name="submitButton" value="<?=$this->transEscAttr('checkout_confirm')?>">
  </div>
</form>
