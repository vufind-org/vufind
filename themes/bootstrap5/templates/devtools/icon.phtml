<?php
  $title = $this->translate('Icons');
  $titleEsc = $this->escapeHtml($title);
  $this->headTitle($title);
  $this->layout()->breadcrumbs = '<li class="breadcrumb-item"><a href="' . $this->url('devtools-home') . '">Development Tools</a>'
  . '<li class="breadcrumb-item active" aria-current="page">' . $titleEsc . '</li>';
?>
<h1><?=$titleEsc?></h1>
<table class="table table-striped" style="width: fit-content;text-align: end">
  <thead>
    <tr>
      <th>Index</th>
      <th>Alias</th>
      <th>Icon</th>
    </tr>
  </thead>
  <tbody>
    <?php foreach ($aliases as $index => $icon): ?>
      <tr>
        <td><?=$index ?></td>
        <td><?=$this->escapeHtml($icon)?></td>
        <td style="text-align: center"><?=$this->icon($icon)?></td>
      </tr>
    <?php endforeach; ?>
  </tbody>
</table>

