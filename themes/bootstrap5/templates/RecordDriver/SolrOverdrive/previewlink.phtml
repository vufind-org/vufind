<?php
$previews = $this->driver->getPreviewLinks();
if ($previews->url): ?>
    <a href="<?=$this->escapeHtmlAttr($previews->url)?>>" target="_blank" class="btn btn-info btn-xs"><?=$this->transEsc('Preview')?></a>
<?php endif; ?>
