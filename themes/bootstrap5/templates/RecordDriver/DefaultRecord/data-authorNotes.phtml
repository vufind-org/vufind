<?php
  $isbn = $this->driver->getCleanISBN();
  $authorNotes = empty($isbn) ? [] : $this->authorNotes($isbn);
?>
<?php if (!empty($authorNotes)): ?>
  <?php foreach ($authorNotes as $provider => $list): ?>
    <?php foreach ($list as $field): ?>
      <?=$field['Content']?><br>
    <?php endforeach; ?>
  <?php endforeach; ?>
<?php endif; ?>
