<?php
    // Set up breadcrumbs:
    $this->breadcrumbs()->set($this->translate('Install VuFind'), active: true);

    // Set up styles:
    $this->assetManager()->appendStyleString(
        ".pre {\n"
        . "  white-space:pre-wrap; width:90%; overflow-y:visible; padding:8px; margin:1em 2em; background:#EEE; border:1px dashed #CCC;\n"
        . "}\n"
    );
?>
<?=$this->component('page-title', ['title' => $this->translate('Install VuFind')]);?>
<?=$this->flashmessages()?>
<p>Save this SQL somewhere safe:</p>

<textarea class="pre" rows="20" readonly id="sql_text"><?=trim($this->sql) ?></textarea>

<form method="post" action="<?=$this->url('install-showsql')?>">
    <input type="submit" name="continue" value="Next" class="btn btn-primary">
</form>

<?php
$script = <<<JS
    $('#sql_text').click(function(e) {
      this.select();
    });
    JS;
?>
<?=$this->assetManager()->outputInlineScriptString($script);
