<h2><?=$this->transEsc('relais_request')?></h2>

<div id="requestMessage">
  <?=$this->icon('spinner') ?>
  <?=$this->transEsc('relais_checking')?>
</div>

<div id="requestButton"></div>

<?php
  $this->assetManager()->appendScriptLink('relais.js');
  $js = <<<JS
      VuFind.getPromise('document-ready').then(() => {
        VuFind.relais.addItem('{$oclc}', '{$failLink}');
      });
      JS;

  echo $this->assetManager()->outputInlineScriptString($js);
?>
