<?php
    // Set up page title:
    $this->headTitle($this->translate('Developer::settings'));

    // Set up breadcrumbs:
    $this->breadcrumbs()->set($this->translate('Your Account'), $this->url('myresearch-home'))
      ->add($this->translate('Developer::api_key'), active: true);
?>
<h2><?=$this->transEsc('Developer::settings')?></h2>
<table class="table table-striped">
    <tr>
        <th class="vertical-center">
            <?=$this->transEsc('Developer::api_key')?>
        </th>
        <td class="flex align-center justify-end">
            <div>
                <?php if ($this->apiKey?->isRevoked()): ?>
                    <span><?=$this->transEsc('Developer::api_key_locked');?></span>
                <?php elseif (!$this->userValid): ?>
                     <span><?=$this->transEsc('Developer::verify_email_address');?></span>
                <?php else: ?>
                    <a class="btn btn-primary" href="<?=$this->url('apikey-generate')?>"><?=$this->transEsc('Developer::api_key_generate')?></a>
                    <?php if ($this->apiKey): ?>
                        <a class="btn btn-secondary" href="<?=$this->url('apikey-delete')?>"><?=$this->transEsc('Developer::api_key_delete')?></a>
                    <?php endif; ?>
                <?php endif; ?>
            </div>
        </td>
    </tr>
</table>
