<!-- finc: search - bulk-action-buttons -->
<?php
/**
origin: vufind/BS3
called by view helper/controller:
usage:
 * shows action button bars on top/below lists
 *
modified for finc:
 * adds 'hidden-print' class
 * adds aria-label #23430
 * improves label for myresearchCheckAll, changes translation of 'select_page' and removes 'with_selected' #19676
configured in: {}
 */
?>
<?php if (isset($this->showCheckboxes) && $this->showCheckboxes): ?>
  <?php /* finc: adds aria-label #23430; also uses 'hidden-print' */ ?>
  <nav class="bulkActionButtons hidden-print" aria-label="<?=$this->transEscAttr('bulk_action_navigation')?>">
    <div class="bulk-checkbox">
      <input type="checkbox" class="checkbox-select-all" name="selectAll" id="<?=$this->idPrefix?>addFormCheckboxSelectAll"<?php if ($this->formAttr): ?> form="<?=$this->escapeHtmlAttr($this->formAttr) ?>"<?php endif; ?>>
      <label for="<?=$this->idPrefix?>addFormCheckboxSelectAll">
        <?php /* finc: improves label for myresearchCheckAll, changes translation of 'select_page' and removes 'with_selected' #19676 */ ?>
        <?=$this->transEsc('select_page')?>:
      </label>
    </div>
    <ul class="action-toolbar">
      <?php if (isset($this->showBulkOptions) && $this->showBulkOptions): ?>
        <li>
          <?php /* keep id="<?=$this->idPrefix?>... on all buttons for proper id assignment for top and bottom toolbars */
                /* finc: adds 'sr-only' spans and aria-hides the standard translation to avoid redundancy;
                make sure, this is (as far as meaningful) identical to 'myresearch/bulk-action-buttons.phtml'
                and 'cart.phtml', CK */ ?>
          <button id="<?=$this->idPrefix?>ribbon-email" class="toolbar-btn btn-type-email" type="submit" name="email" value="1"<?php if ($this->formAttr): ?> form="<?=$this->escapeHtmlAttr($this->formAttr) ?>"<?php endif; ?>>
            <span class="sr-only"><?=$this->transEscAttr('bookbag_email_selected')?></span>
            <span aria-hidden="true"><?=$this->transEsc('Email') ?></span>
          </button>
        </li>
        <?php $exportOptions = $this->export()->getActiveFormats('bulk'); if (count($exportOptions) > 0): ?>
          <li><?php /* finc: adds sr-only spans and aria-hides standard translation to avoid redundancy */ ?>
            <button id="<?=$this->idPrefix?>ribbon-export" class="toolbar-btn btn-type-export" type="submit" name="export" value="1" <?php if ($this->formAttr): ?> form="<?=$this->escapeHtmlAttr($this->formAttr) ?>"<?php endif; ?>>
              <span class="sr-only"><?=$this->transEscAttr('bookbag_export')?></span>
              <span aria-hidden="true"><?=$this->transEsc('Export')?></span>
            </button></li>
        <?php endif; ?>
        <li><?php /* finc: adds sr-only spans and aria-hides standard translation to avoid redundancy */ ?>
          <button id="<?=$this->idPrefix?>ribbon-print" class="toolbar-btn btn-type-print" type="submit" name="print" value="1"<?php if ($this->formAttr): ?> form="<?=$this->escapeHtmlAttr($this->formAttr) ?>"<?php endif; ?>>
            <span class="sr-only"><?=$this->transEscAttr('bookbag_print')?></span>
            <span aria-hidden="true"><?=$this->transEsc('Print')?></span>
          </button></li>
        <?php if ($this->userlist()->getMode() !== 'disabled'): ?>
          <li><?php /* finc: adds sr-only spans and aria-hides standard translation to avoid redundancy */ ?>
            <button id="<?=$this->idPrefix?>ribbon-save" class="toolbar-btn btn-type-save" type="submit" name="saveCart" value="1" <?php if ($this->formAttr): ?> form="<?=$this->escapeHtmlAttr($this->formAttr) ?>"<?php endif; ?>>
              <span class="sr-only"><?=$this->transEscAttr('bookbag_save_selected')?></span>
              <span aria-hidden="true"><?=$this->transEsc('Save')?></span>
            </button></li>
        <?php endif; ?>
      <?php endif; ?>
      <?php if (isset($this->showCartControls) && $this->showCartControls): ?>
        <li><?php /* finc: adds data-selector="true" to prevent creation of empty titles; see also finc/templates/myresearch/bulk-action-buttons; Fixme: Do we have a better solution? CK */
                /* finc: adds sr-only spans and aria-hides standard translation to avoid redundancy */ ?>
          <button id="<?=$this->idPrefix?>updateCart" type="submit" class="toolbar-btn btn-type-add" name="add" value="1"<?php if ($this->formAttr): ?> form="<?=$this->escapeHtmlAttr($this->formAttr) ?>"<?php endif; ?> data-selector="true">
            <span class="sr-only"><?=$this->transEscAttr('bookbag_add_selected')?></span>
            <span aria-hidden="true"><?=$this->transEsc('Add to Book Bag')?></span>
          </button></li>
      <?php endif; ?>
    </ul>
  </nav>
<?php endif; ?>
<!-- finc: search - bulk-action-buttons - END -->
