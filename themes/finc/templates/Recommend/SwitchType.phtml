<!-- finc: Recommend - SwitchType -->
<?php
/**
origin: vufind/BS3
called by view helper/controller:
usage:
modified for finc:
 * uses  <p> and aria-live for alerts #17950, #21622
 * FIXME: remove file in finc, if the changes are available in VuFind; create PR if not yet done
configured in: {}
 */
?>

<?php if ($handler = $this->recommend->getNewHandler()): ?>
  <?php /* finc: uses <p> and aria-live for alerts #17950, #21622 */ ?>
  <p class="alert alert-info" aria-live="polite">
    <?=$this->transEsc('widen_prefix')?>
    <a href="<?=$this->recommend->getResults()->getUrlQuery()->setHandler($handler)?>"><?=$this->transEsc($this->recommend->getNewHandlerName())?></a>.
  </p>
<?php endif; ?>
<!-- finc: Recommend - SwitchType - END -->
