<!-- finc: recordtab - descriptionlido -->
<?php
/**
origin: VuFind/RecordTab/description.phtml
called by view helper/controller:
usage:
modified for finc-descriptionlido:
 * uses 'description-lido' instead of 'description'
 * adds code for responsive data table, adds span to caption
 * adds empty lang (='undefined') tag
 * adds data-title
configured in: {}
 */
?>
<?php
    // Set page title.
    $this->headTitle($this->translate('Description') . ': ' . $this->driver->getBreadcrumb());

    $formatter = $this->recordDataFormatter();
    // finc-descriptionlido: uses'description-lido' instead of 'description'
    $mainFields = $formatter->getData($driver, $formatter->getDefaults('description-lido'));
?>
<?php /* finc-descriptionlido adds code for responsive data table, adds span to caption */ ?>
<table class="table table-striped table-resp-data">
  <caption><span class="sr-only"><?=$this->transEsc('Description')?></span></caption>
  <?php if (!empty($mainFields)): ?>
    <?php foreach ($mainFields as $current): ?>
      <?php /* finc-descriptionlido adds empty lang (='undefined') tag */ ?>
      <?php /* finc-descriptionlido adds data-title */ ?>
      <tr><th><?=$this->transEsc($current['label'])?>:</th><td lang="" data-title="<?= $this->transEscAttr($current['label']) ?>:"><?=$current['value']?></td></tr>
    <?php endforeach; ?>
  <?php else: ?>
    <tr><td><?=$this->transEsc('no_description')?></td></tr>
  <?php endif; ?>
</table>
<!-- finc: recordtab - descriptionlido - END -->