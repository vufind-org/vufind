<? $activeFiltersCount = $this->recommend->getActiveFacetsCount();
if ($activeFiltersCount > 0): ?>
  <div class="alert alert-info">
<?=$this->transEsc('nohit_active_filters', array('%%activeFiltersCount%%' => $activeFiltersCount))?> 
<a href="<?=$this->recommend->getResults()->getUrlQuery()->removeAllFilters()?>"><?=$this->transEsc('nohit_query_without_filters')?></a>
  </div>
<? endif; ?>