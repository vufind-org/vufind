<?php if ($this->disabled): ?>
  <h3><?=$this->transEsc('explain_disabled', ['%%searchClassId%%' => $this->driver->getSourceIdentifier()])?></h3>
<?php else: ?>
  <?=
    $this->record($this->driver)->renderTemplate(
        'explain.phtml',
        [
          'driver' => $this->driver,
          'explanation' => $this->explanation,
        ]
    );
  ?>
<?php endif; ?>
