<?php foreach ($this->driver->getSummary() as $summary): ?>
  <?=$this->escapeOrCleanHtml($summary, allowHtml: true) ?><br>
<?php endforeach; ?>
<?php $isbn = $this->driver->getCleanISBN(); ?>
<?php foreach ($this->summaries($isbn) as $provider => $content): ?>
  <?=
    implode(
        '<br>',
        array_map(
            function ($summary) {
                $htmlContent = str_starts_with($summary, '<') && str_ends_with($summary, '>');
                return $htmlContent ? $summary : ($this->escapeOrCleanHtml($summary, allowHtml: true));
            },
            $content
        )
    )
  ?>
<?php endforeach; ?>
