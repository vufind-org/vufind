<?
  // Set page title.
  $this->headTitle($this->translate('Permission Denied'));

  // Disable top search box -- this page has a special layout.
  $this->layout()->searchbox = false;

  $this->layout()->breadcrumbs = '<li class="active">Error</li>';
?>
<div class="alert alert-danger">
  <h2><?=$this->transEsc('Permission Denied')?></h2>
  <p>
    <?=$this->transEsc('permission_denied')?>
    <?=$this->transEsc('permission_action_list_header')?>
  </p>
  <p>
    <?=$this->flashmessages()?>
  </p>
  <p>
    <?=$this->transEsc('Please contact the Library Reference Department for assistance')?>
    <br/>
    <? $supportEmail = $this->escapeHtmlAttr($this->systemEmail()); ?>
    <a href="mailto:<?=$supportEmail?>"><?=$supportEmail?></a>
  </p>
</div>
