<?
    // Set up page title:
    $this->headTitle($this->translate('Channels'));

    // Set up breadcrumbs:
    $this->layout()->breadcrumbs = '<li class="active">' . $this->transEsc('Channels') . '</li>';
?>
<form action="<?=$this->url('channels-search')?>">
  Search for more channels:
  <input type="text" name="lookfor" />
  <input type="submit" />
</form>
<?=$this->render('channels/channelList.phtml')?>
