<h2><?=$this->transEsc('relais_request')?></h2>

<div class="row">
  <div class="col-md-12" id="requestMessage">
    <i class="fa fa-spinner fa-spin"></i>
    <?=$this->transEsc('relais_checking')?>
  </div>
</div>

<div class="row">
   <div class="col-md-12" id="requestButton">
   </div>
</div>

<?php
  $failLink = $this->escapeJs($this->config()->get('config')->Relais->loginUrl ?? '#');
  $this->headScript()->appendFile('relais.js');
  $activateRelais = "$(document).ready(function () { relaisAddItem('$oclc', '$failLink'); });\n";
  echo $this->inlineScript(\Zend\View\Helper\HeadScript::SCRIPT, $activateRelais, 'SET')
?>
