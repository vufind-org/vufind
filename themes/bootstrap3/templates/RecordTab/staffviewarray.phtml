<?
    // Set page title.
    $this->headTitle($this->translate('Staff View') . ': ' . $this->driver->getBreadcrumb());
?>
<table class="citation table table-striped">
  <? $fields = $this->driver->getRawData(); foreach ($fields as $field => $values): ?>
    <tr>
      <th><? if (isset($fields["__unprocessed_$field"])): ?><?=$this->escapeHtml("__unprocessed_$field")?> => <? endif; ?><?=$this->escapeHtml($field)?></th>
      <td>
        <? if (!is_array($values)) { $values = array($values); } ?>
        <? foreach ($values as $value): ?>
          <?=$this->escapeHtml(is_array($value) ? print_r($value, true) : $value)?><br />
        <? endforeach; ?>
      </td>
    </tr>
  <? endforeach; ?>
</table>