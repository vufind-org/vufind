<? $options = $this->searchOptions($this->searchClassId); ?>
<? $searchTabs = $this->searchtabs($this->searchClassId, $this->lookfor, $this->searchIndex, $this->searchType, isset($this->results) ? $this->results->getParams()->getHiddenFilters() : $options->getLastHiddenFilters()); ?>
<? if (count($searchTabs) > 0): ?>
  <ul class="nav nav-tabs">
    <? foreach ($searchTabs as $tab): ?>
      <li<?=$tab['selected'] ? ' class="active"' : ''?>>
        <a <?=$tab['selected'] ? '' : 'href="' . $this->escapeHtmlAttr($tab['url']) . '"' ?>><?=$this->transEsc($tab['label']); ?></a>
      </li>
    <? endforeach; ?>
  </ul>
<? endif; ?>