<?
$that = $this;
$translator = function ($in) use ($that) {
    return $that->transEsc('HoldingStatus::service_' . $in);
};
$serviceList = implode(', ', array_map($services, $translator));
?>
<span class="label label-success">
  <?=count($services) == 1 ? $this->transEsc('HoldingStatus::service_available_'.array_pop($services)) : $this->transEsc('HoldingStatus::services_available', ['%%list%%' => $serviceList]) ?>
</span>