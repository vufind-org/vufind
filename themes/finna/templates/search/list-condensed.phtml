<!-- START of: finna - search/list-condensed.phtml -->
<? if (!isset($this->indexStart)) $this->indexStart = 0; ?>
<? $showCheckboxes = !isset($this->hideCartControls) && $this->cart()->isActive() ?>
<? $i = $this->indexStart; foreach ($this->results->getResults() as $current):
  $recordNumber = $this->results->getStartRecord()+$i-$this->indexStart; ?>
  <div id="result<?=$i++ ?>" class="row result condensed-list-view clearfix">
    <div class="col-xs-1 hidden-print<? if ($showCheckboxes): ?> checkbox<? endif; ?> sr-only">
      <label>
        <? if ($showCheckboxes): ?>
          <?=$this->record($current)->getCheckbox()?>
        <? endif; ?>
        <?=$recordNumber?>
      </label>
    </div>
    <?=$this->record($current)->getSearchResult('condensed')?>
  </div>
<? endforeach; ?>
<!-- END of: finna - search/list-condensed.phtml -->
