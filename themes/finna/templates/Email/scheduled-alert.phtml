<?= $this->translate('New results found for search') ?>:
  <?= empty($info['description']) ? $this->translate('history_empty_search') : $this->escapeHtml($info['description'])?>

<? if (count($info['filters'])): ?>

<?= $this->translate('Limits') ?>:
<? foreach ($info['filters'] as $format => $filters): ?>
  <?= $this->translate($format) . ': ' ?>
<? $index = 0; ?>
<? foreach ($filters as $f): ?>
<?= ($index++ > 0 ? ' ' . $this->translate($f['operator']) . ' ' : '') . $this->translate($f['value']); ?>
<? endforeach; ?>

<? endforeach; ?>
<? endif; ?>

<?= $this->translate('Link to full results') ?>:
<?= $info['url'] ?>



<?= $info['recordCount'] . ' ' . $this->translate('Newest Results') ?>:

<? foreach ($records as $result): ?>
<?= $this->record($result)->getSearchResult('email') . PHP_EOL; ?>
<? endforeach; ?>


<?= $this->translate('unsubscribe_description') ?>:
<?= $info['unsubscribeUrl'] ?>
