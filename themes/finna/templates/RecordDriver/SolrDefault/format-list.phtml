<? $unprocessedFormats = $this->driver->getUnprocessedFormat(); ?>
<? $formats = $this->driver->getFormats(); if ($formats): ?>
  <span class="iconlabel format-<?=$this->record($this->driver)->getFormatClass(end($unprocessedFormats))?>"><?=$this->transEsc(end($formats))?></span>
<? endif; ?>
