<!-- START of: finna - Auth/MozillaPersona/login.phtml -->
<div class="persona-auth">
    <? $account = $this->auth()->getManager(); ?>
    <?=$this->auth()->getLoginFields()?>

    <? // If page load done and we are showing Lightbox window,
       // Mozilla Persona login link must be updated. ?>
    <script>
    if ($.isReady === true) {
        if (typeof finna.persona !== 'undefined') {
            finna.persona.setupLoginLinks();
        }
    }
    </script>
</div>
<!-- END of: finna - Auth/MozillaPersona/login.phtml -->
