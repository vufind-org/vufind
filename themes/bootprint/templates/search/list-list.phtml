<? $i = 0; foreach ($this->results->getResults() as $current):
  $recordNumber = $this->results->getStartRecord()+$i; ?>
  <div id="result<?=$i++ ?>" class="row-fluid clearfix result">
    <strong class="checkbox pull-left"><?=$recordNumber?></strong>
    <? if ($this->cart()->isActive()): ?><?=$this->record($current)->getCheckbox()?><? endif; ?>
    <?=$this->record($current)->getSearchResult('list')?>
  </div>
<? endforeach; ?>